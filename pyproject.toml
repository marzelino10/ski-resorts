[project]
name = "ski-resorts"
version = "0.1.0"
description = "Ski Resorts data engineering pipeline using AWS, Snowflake, and dbt"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.2",
    "dbt-snowflake>=1.11.1",
]
